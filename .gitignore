# Credentials — never commit
token.json
credentials.json

# Config with real secrets (Wedos deployment — keep config.php committed as template)
# Uncomment if you fill in real values directly in config.php:
# config.php

# macOS
.DS_Store

# PHP / editor artefacts
*.php~
*.php.bak

# Python leftovers (in case venv is restored locally)
venv/
__pycache__/
*.pyc
.env
