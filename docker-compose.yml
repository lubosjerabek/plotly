version: '3.8'

services:
  plotly-app:
    build: .
    container_name: plotly-app
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
