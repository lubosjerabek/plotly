# config.php lives on the server with real credentials — never overwrite it
config.php

# One-time files — only needed for first setup, then deleted from server
schema.sql
setup.php

# Docker / CI artefacts — not needed on shared hosting
Dockerfile
docker-compose.yml
.dockerignore
.github/

# Version control + docs
.git/
.gitignore
README.md

# Dev environment
venv/
__pycache__/
*.pyc
.DS_Store

# Credentials
credentials.json
token.json

# Tests
tests/

# Claude Code workspace
.claude/
.pytest_cache/
